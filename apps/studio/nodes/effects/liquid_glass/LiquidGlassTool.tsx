import { NodeType } from '@blackboard/types';
import { NodeToolButton } from '../../NodeToolButton';

export const LiquidGlassTool = () => {
  return <NodeToolButton nodeType={NodeType.LIQUID_GLASS} />;
};
